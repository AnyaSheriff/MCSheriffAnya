// Заглушка TCP-листенера.
//
// В будущем здесь будет реализован полноценный TCP-листенер,
// вынесенный из main.rs: приём подключений, разбор пакетов протокола
// Minecraft (VarInt-длины, состояния handshake/status/login/play),
// и передача обработки в соответствующие модули (player, world, entity).
//
// На текущем этапе (шаг 1) базовая логика приёма подключений
// временно находится непосредственно в src/main.rs.
